show databases;

use essp;

show tables;

create table photos (PhotoId int UNSIGNED primary key NOT NULL AUTO_INCREMENT, UserID int UNSIGNED NOT NULL, Location varchar(500) NOT NULL, Updated_by varchar(50) NOT NULL, Updated_on TIMESTAMP);

describe photos;


alter table photos add constraint FOREIGN KEY (UserId) References users(UserId);
